# Rules for Writing Code

1. The No.1 priority is not to use `any` type. It should be avoided at all costs and should be replaced with proper types.
2. Follow CamelCase naming convention for variables, functions. For Classes, use PascalCase.
3. Use Zod for validation and inferring types.