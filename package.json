{
	"name": "fluxify",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"dev": "bun --parallel 'bun run dev:server' 'bun run dev:worker' 'bun run dev:telemetry' 'bun run dev:portal' 'bun run dev:ai' 'bun run dev:docs'",
		"dev:server": "bun run --env-file=.env --watch ./apps/server/deployments/standalone.ts",
		"dev:worker": "bun run --env-file=.env --watch ./apps/server/deployments/compiledWorker.ts",
		"dev:worker:dag": "bun run --env-file=.env --watch ./apps/server/deployments/worker.ts",
		"dev:telemetry": "bun run --env-file=.env --watch ./apps/server/deployments/telemetryWorker.ts",
		"dev:portal": "turbo run dev --filter=@fluxify/portal",
		"dev:ai": "bun run --env-file=.env --watch ./apps/ai-gateway/src/server.ts",
		"dev:docs": "vitepress dev docs",
		"build": "turbo run build",
		"lint": "turbo run lint",
		"test:unit": "bun test --timeout 300000 --path-ignore-patterns \"**/*.test.ts\"",
		"test:integration": "bun test --parallel --timeout 300000 --path-ignore-patterns \"**/*.spec.ts\" --path-ignore-patterns \"testing/**\"",
		"test:lib": "bun run --parallel --cwd packages/lib test",
		"test:blocks": "bun run --parallel --cwd packages/blocks test",
		"test:adapters": "bun run --parallel --cwd packages/adapters test",
		"test:server:unit": "bun test --parallel --cwd apps/server --path-ignore-patterns \"**/*.test.ts\"",
		"test:server:integration": "bun test --parallel --cwd apps/server --path-ignore-patterns \"**/*.spec.ts\"",
		"test:e2e": "bun test --cwd testing/e2e --timeout 600000",
		"test:portal": "bun test --parallel --cwd apps/portal",
		"test:ai-gateway": "bun test --parallel --cwd apps/ai-gateway",
		"test:ai": "bun run test:ai-gateway",
		"analyze": "bun x fta-cli --score-cap 70 .",
		"security:scan": "bun x secretlint --format compact \"**/*\"",
		"docs:sync-agent-docs": "bun run scripts/sync-agent-docs.ts",
		"docs:build": "bun run docs:sync-agent-docs && vitepress build docs",
		"docs:preview": "vitepress preview docs",
		"db:generate": "bun --env-file=../../.env --cwd ./apps/server drizzle-kit generate",
		"db:migrate": "bun --env-file=../../.env --cwd ./apps/server drizzle-kit push",
		"prepare": "bun run scripts/setup-hooks.ts"
	},
	"//scripts": {
		"dev:worker": "Compiled worker — runs routes as generated JavaScript. This is the default execution engine.",
		"dev:worker:dag": "LEGACY graph interpreter. Kept only until the compiled worker clears manual testing, then removed.",
		"build:ai": "Removed — `bun run build` already builds the AI gateway through turbo."
	},
	"keywords": [
		"fluxify",
		"@fluxify"
	],
	"workspaces": [
		"packages/*",
		"apps/*",
		"testing/e2e"
	],
	"packageManager": "bun@1.4.0",
	"author": "07prajwal2000",
	"license": "Apache-2.0",
	"description": "",
	"dependencies": {
		"@orama/orama": "^3.1.18",
		"better-auth": "1.6.23",
		"bson": "7.2.0",
		"http-proxy": "^1.18.1"
	},
	"devDependencies": {
		"@faker-js/faker": "^10.3.0",
		"@secretlint/secretlint-rule-preset-recommend": "^13.0.4",
		"@types/bun": "^1.4.0",
		"@types/dockerode": "^4.0.1",
		"@typescript/native-preview": "^7.0.0-dev.20260219.1",
		"dockerode": "^4.0.9",
		"fta-cli": "^3.0.0",
		"mermaid": "^11.16.0",
		"secretlint": "^13.0.4",
		"turbo": "^2.5.5",
		"ui-ux-pro-max-cli": "^2.10.2",
		"vitepress": "^1.6.4",
		"vitepress-plugin-mermaid": "^2.0.17"
	},
	"trustedDependencies": [
		"@faker-js/faker",
		"@swc/core",
		"@typescript/native-preview",
		"esbuild",
		"sharp"
	],
	"resolutions": {
		"bson": "7.2.0",
		"ansi-regex": "5.0.1"
	},
	"overrides": {
		"ansi-regex": "5.0.1",
		"bson": "7.2.0"
	}
}
