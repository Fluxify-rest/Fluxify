{
  "name": "@fluxify/adapters",
  "version": "1.0.0",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "dev": "bun --watch index.ts --outdir dist --bundle --target bun --minify --external better-sqlite3 --external mysql2 --external sqlite3 --external mysql --external pg-query-stream --external tedious --external oracledb",
    "lint": "tsc --noEmit",
    "build": "bun build index.ts --outdir dist --bundle --target bun --minify --external better-sqlite3 --external mysql2 --external sqlite3 --external mysql --external pg-query-stream --external tedious --external oracledb"
  },
  "dependencies": {
    "@fluxify/lib": "workspace:../lib",
    "knex": "^3.1.0",
    "pg": "^8.16.3",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
