{
  "name": "@fluxify/blocks",
  "version": "1.0.0",
  "keywords": [],
  "files": [
    "dist"
  ],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.2.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "test": "vitest run",
    "dev": "bun --watch index.ts --outdir dist --bundle --target bun --minify",
    "lint": "tsc --noEmit",
    "build": "bun build index.ts --outdir dist --bundle --target bun --minify"
  },
  "dependencies": {
    "@fluxify/adapters": "workspace:../adapters",
    "@fluxify/lib": "workspace:../lib",
    "dayjs": "^1.11.19",
    "zod": "^4.0.17"
  }
}
